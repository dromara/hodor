spring:
  application:
    name: hodor-scheduler
  output:
    ansi:
      enabled: always
  profiles:
    active: core

hodor:
  netServer:
    appName: hodor
    host: 127.0.0.1
    port: 8081
  registry:
    servers: 127.0.0.1:2181
    namespace: "hodor-test"